{
  "annotations": { "list": [] },
  "editable": false,
  "panels": [
    {
      "type": "stat",
      "title": "Request Rate (req/s)",
      "id": 1,
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum(rate(http_requests_total[1m]))", "refId": "A" }
      ]
    },
    {
      "type": "graph",
      "title": "Latency p95 (s)",
      "id": 2,
      "datasource": "Prometheus",
      "targets": [
        { "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))", "refId": "A" }
      ],
      "lines": true
    },
    {
      "type": "stat",
      "title": "Error Rate (%)",
      "id": 3,
      "datasource": "Prometheus",
      "targets": [
        { "expr": "100 * (sum(rate(http_requests_total{status=~\"5..|4..\"}[1m])) / sum(rate(http_requests_total[1m])))", "refId": "A" }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["auth","backend"],
  "time": { "from": "now-6h", "to": "now" },
  "title": "Auth Backend — Overview",
  "version": 1
}
